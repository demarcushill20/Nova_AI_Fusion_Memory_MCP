"""Standalone utility scripts for Fusion Memory MCP ops.

Modules under this package are CLI tools, not runtime dependencies of the
Fusion Memory MCP service. They are safe to import for typing/tests but must
not be wired into ``app.services`` production paths.
"""
