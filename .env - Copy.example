# Environment variables for Nova AI Memory MCP Server

# OpenAI Configuration
OPENAI_API_KEY="sk-..."

# Pinecone Configuration
PINECONE_API_KEY="..."
PINECONE_ENV="..."
# PINECONE_INDEX="nova-ai-memory" # Optional: Defaults to nova-ai-memory if not set

# Neo4j Configuration
# NEO4J_URI="bolt://neo4j:7687" # Optional: Defaults to bolt://neo4j:7687 for Docker Compose
# NEO4J_URI="bolt://localhost:7687" # Use this when running services locally outside Docker
# NEO4J_USER="neo4j" # Optional: Defaults to neo4j if not set
NEO4J_PASSWORD="..." # Required if Neo4j auth is enabled
# NEO4J_DATABASE="neo4j" # Optional: Defaults to neo4j if not set

# Reranker Configuration (Optional)
# RERANKER_MODEL_NAME="cross-encoder/ms-marco-MiniLM-L-6-v2" # Optional: Defaults to this model if not set

# Embedding Model Configuration (Optional)
# EMBEDDING_MODEL="text-embedding-ada-002" # Optional: Defaults to this model if not set
